{
  "name": "fringe",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "pnpm run lint:server && pnpm run lint:client && pnpm run lint:infra",
    "lint:server": "dotnet format fringe.sln --verify-no-changes",
    "lint:client": "pnpm --dir fringe-client run lint",
    "lint:infra": "pnpm --prefix infra run lint",
    "format": "pnpm run format:server && pnpm run format:client && pnpm run format:infra",
    "format:server": "dotnet format fringe.sln --verify-no-changes",
    "format:client": "pnpm --dir fringe-client run format:check",
    "format:infra": "pnpm --prefix infra run format:check",
    "test": "./scripts/test-all.sh",
    "test:server": "dotnet test fringe.sln --collect:\"XPlat Code Coverage\" --results-directory coverage/dotnet --logger trx",
    "test:client": "pnpm --dir fringe-client run test:coverage",
    "test:infra": "pnpm --prefix infra run test:coverage"
  }
}
